import { motion } from "framer-motion";

export default function Portfolio() {
  return (
    <div className="min-h-screen bg-gray-50 text-gray-800">
      {/* Hero Section */}
      <section className="flex flex-col items-center justify-center h-screen text-center p-6 bg-gradient-to-b from-pink-100 to-white">
        <motion.h1
          initial={{ opacity: 0, y: -20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6 }}
          className="text-5xl font-bold mb-4"
        >
          Anup Photography
        </motion.h1>
        <p className="text-lg text-gray-600 max-w-xl">
          Capturing moments from today… Creating memories for a lifetime.
        </p>
      </section>

      {/* Gallery Section */}
      <section id="gallery" className="p-10">
        <h2 className="text-3xl font-semibold text-center mb-8">My Clicks</h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
          {/* Replace image URLs with your own photos */}
          {[1, 2, 3, 4, 5, 6].map((i) => (
            <motion.div
              key={i}
              whileHover={{ scale: 1.05 }}
              className="overflow-hidden rounded-2xl shadow-lg"
            >
              <img
                src={`https://source.unsplash.com/random/800x600?sig=${i}&nature`}
                alt={`Gallery ${i}`}
                className="w-full h-60 object-cover"
              />
            </motion.div>
          ))}
        </div>
      </section>

      {/* About Section */}
      <section id="about" className="p-10 bg-white text-center">
        <h2 className="text-3xl font-semibold mb-4">About Me</h2>
        <p className="max-w-2xl mx-auto text-gray-600">
          Hi, I am Anup 👋, a passionate photographer who loves capturing nature,
          people, and emotions through the lens. Photography for me is not just a
          hobby but a way to tell stories that words cannot express.
        </p>
      </section>

      {/* Contact Section */}
      <section id="contact" className="p-10 bg-gray-100 text-center">
        <h2 className="text-3xl font-semibold mb-4">Contact Me</h2>
        <p className="text-gray-600 mb-4">
          Interested in working together or want to buy my photos?
        </p>
        <a
          href="mailto:your-email@example.com"
          className="bg-pink-500 text-white px-6 py-3 rounded-xl shadow-lg hover:bg-pink-600"
        >
          Email Me
        </a>
      </section>

      {/* Footer */}
      <footer className="text-center py-6 text-gray-500 text-sm">
        © {new Date().getFullYear()} Anup Photography. All rights reserved.
      </footer>
    </div>
  );
}
